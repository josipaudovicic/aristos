package com.example.backend.korisnik.HelpingTables;

import org.springframework.data.jpa.repository.JpaRepository;

public interface QualifiedForRepository extends JpaRepository<QualifiedFor, QualifiedForId> {
}
